{
  "name": "@douglasduteil/framework.docs",
  "description": "Framework Docs",
  "license": "WTFPL",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "precommit": "yarn workspaces foreach run precommit",
    "lint": "yarn workspaces foreach run lint",
    "build": "yarn workspaces foreach run build",
    "test": "yarn workspaces foreach run test",
    "docs": "dd-frameword-docs docs/features"
  },
  "devDependencies": {
    "@douglasduteil/framework.docs.cli": "link:./targets/cli"
  },
  "workspaces": [
    "modules/*",
    "targets/*"
  ]
}
